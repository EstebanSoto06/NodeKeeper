# Server
PORT=4000
NODE_ENV=development

# Database
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/nodekeeper?schema=public"

# Auth
JWT_SECRET="replace_with_a_secure_local_secret"
JWT_EXPIRES_IN="8h"

# Uploads
UPLOAD_DIR="uploads/evidences"
MAX_FILE_SIZE_MB=10
